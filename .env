# Supabase — these are PUBLIC client values, safe to commit and ship in the
# browser bundle. All real protection is enforced server-side by Row Level
# Security policies. The powerful service-role key is NOT here — it lives only
# inside the `admin-users` Edge Function on Supabase.
VITE_SUPABASE_URL=https://mhwlivofbcxqwoedgikk.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_RK1LhY5jfqxJoYgZwWbPaw_TTDmJoVk
